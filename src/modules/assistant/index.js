const routes = require('./assistant.routes');
const service = require('./assistant.service');
const controller = require('./assistant.controller');

module.exports = {
  routes,
  service,
  controller,
};
